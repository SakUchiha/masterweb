{
  "name": "vibing",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd vibing/code-understanding-app && npm install && npm run build:frontend",
    "start": "vercel dev"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
