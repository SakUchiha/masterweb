{
  "name": "vibing",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd vibing/code-understanding-app && npm install && npm run build:frontend",
    "start": "vercel dev"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "etag": "^1.8.1",
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": "^22.0.0"
  }
}
